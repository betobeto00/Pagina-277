services:
  - type: web
    name: virtud-y-victoria-277
    runtime: docker
    plan: free
    region: oregon
    dockerfilePath: ./Dockerfile
    dockerContext: .
    healthCheckPath: /wp-login.php
    envVars:
      - key: WORDPRESS_DB_HOST
        value: "{{virtud-db.MYSQL_HOST}}"
      - key: WORDPRESS_DB_PORT
        value: "{{virtud-db.MYSQL_PORT}}"
      - key: WORDPRESS_DB_NAME
        value: "{{virtud-db.MYSQL_DATABASE}}"
      - key: WORDPRESS_DB_USER
        value: "{{virtud-db.MYSQL_USER}}"
      - key: WORDPRESS_DB_PASSWORD
        value: "{{virtud-db.MYSQL_PASSWORD}}"
      - key: WORDPRESS_TABLE_PREFIX
        value: wp_
      - key: WORDPRESS_DEBUG
        value: "false"
      - key: WORDPRESS_CONFIG_EXTRA
        value: "define('WP_HOME','https://virtud-y-victoria-277.onrender.com'); define('WP_SITEURL','https://virtud-y-victoria-277.onrender.com'); define('WP_ENVIRONMENT_TYPE', 'production');"

  - type: pserv
    name: virtud-db
    runtime: docker
    plan: free
    region: oregon
    dockerfilePath: ./Dockerfile.mariadb
    dockerContext: .
    envVars:
      - key: MYSQL_DATABASE
        value: virtud_y_victoria
      - key: MYSQL_USER
        value: virtud_user
      - key: MYSQL_PASSWORD
        value: virtud_pass
      - key: MYSQL_ROOT_PASSWORD
        value: root_pass